import { StatDashboardUiModule } from './stat-dashboard-ui.module';

describe('StatDashboardUiModule', () => {
  it('should work', () => {
    expect(new StatDashboardUiModule()).toBeDefined();
  });
});

import { ProfileSettingsUiModule } from './profile-settings-ui.module';

describe('ProfileSettingsUiModule', () => {
  it('should work', () => {
    expect(new ProfileSettingsUiModule()).toBeDefined();
  });
});
